# Abhi Studio — backend dependencies
# Everything installs from PyPI wheels; no system packages or compilers needed.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
python-multipart>=0.0.9      # file uploads
pydantic>=2.5

# real media engines
pillow>=10.2                 # image generation/edit/thumbnails
numpy>=1.26                  # audio DSP, waveform analysis
imageio-ffmpeg>=0.4.9        # ships a static FFmpeg 7 binary (no apt required)

# provider HTTP clients
httpx>=0.27

# tests
pytest>=8.0
pytest-timeout>=2.3
